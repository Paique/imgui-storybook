package com.lattestudio.imguistorybook.api;

/** Arg types supported by the story Controls panel and the export manifests. */
public enum ArgType {
    STRING,
    BOOLEAN,
    INT,
    FLOAT,
    ENUM,
    /** Color stored as {@code #RRGGBB}. */
    COLOR
}
